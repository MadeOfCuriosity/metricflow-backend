version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip install -r requirements.txt

run:
  command: alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port 8000 --workers 2
  network:
    port: 8000
  env:
    - name: ENVIRONMENT
      value: "production"
    - name: FRONTEND_URL
      value: "https://main.d33ftwd17k5p6o.amplifyapp.com"
  secrets:
    - name: DATABASE_URL
      value-from: "arn:aws:secretsmanager:us-east-2:170690344518:secret:metricflow/databaseurl-0lANQz"
    - name: SECRET_KEY
      value-from: "arn:aws:secretsmanager:us-east-2:170690344518:secret:metricflow/jwt-secret-nG3RlQ"
